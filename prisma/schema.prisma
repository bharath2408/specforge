// Generated by SpecForge — do not edit manually

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id  String @id @default(uuid())
  email  String @unique
  name  String
  role  String @default("user")
  createdAt  DateTime @default(now())
  posts  Post[]
}

model Post {
  id  String @id @default(uuid())
  title  String
  body  String
  published  Boolean @default(false)
  authorId  String // FK to User
  author  User  @relation(fields: [authorId], references: [id])
}

model CustomCommand {
  id  String @id @default(uuid())
  name  String @unique
  description  String
  run  String
  alias  String @unique
  variables  Json
  createdAt  DateTime @default(now())
  updatedAt  DateTime @default(now())
  commandvariables  CommandVariable[]
}

model CommandVariable {
  id  String @id @default(uuid())
  name  String
  required  Boolean @default(true)
  default  String
  description  String
  commandId  String // FK to CustomCommand
  command  CustomCommand  @relation(fields: [commandId], references: [id])
}

model CommandRegistry {
  id  String @id @default(uuid())
  commands  Json
  configPath  String @default(".specforge/commands.yaml")
  loadedAt  DateTime @default(now())
}

