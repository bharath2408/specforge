{
  "name": "@specforge-dev/specforge",
  "version": "1.0.6",
  "description": "Spec-Driven Development Kit",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "test": "pnpm -r test",
    "test:e2e": "npx playwright test",
    "test:e2e:pipeline": "npx playwright test --project pipeline",
    "lint": "pnpm -r lint"
  },
  "keywords": [
    "spec-driven",
    "code-generation",
    "sdlc",
    "toolkit"
  ],
  "author": "Bharath Kumar",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
