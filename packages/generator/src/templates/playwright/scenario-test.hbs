// Generated by SpecForge — do not edit manually
// Scenario tests for {{specName}}
// Source: specs/{{specId}}/spec.md

import { test, expect } from "@playwright/test";
{{#if hasAuth}}
import { getAuthHeaders } from "./auth-setup";
{{/if}}
{{#each pageImports}}
import { {{this.className}} } from "./pages/{{this.fileName}}";
{{/each}}

test.describe("{{specName}}", () => {
{{#each scenarios}}
  // Priority: {{this.priority}}
  test("[{{this.id}}] {{this.description}}", async ({ {{../testArg}} }) => {
    // Given: {{this.given}}
    // TODO: Set up preconditions

    // When: {{this.when}}
    // TODO: Perform the action
{{#if this.isApiScenario}}
    // const response = await request.{{this.httpMethod}}("{{this.suggestedPath}}", {
    //   data: { /* request body */ },
    // });
{{/if}}
{{#if this.isUiScenario}}
    // const page = await context.newPage();
    // await page.goto("{{this.suggestedPath}}");
{{/if}}

    // Then: {{this.then}}
    // TODO: Add assertions
{{#if this.isApiScenario}}
    // expect(response.status()).toBe(200);
    // const body = await response.json();
    // expect(body).toBeDefined();
{{/if}}
{{#if this.isUiScenario}}
    // await expect(page.locator("...")).toBeVisible();
{{/if}}
  });

{{/each}}
});
