// Generated by SpecForge — do not edit manually
// Test data factory for {{modelName}}

export interface {{modelName}}TestData {
{{#each fields}}
  {{@key}}{{#if this.optional}}?{{/if}}: {{this.tsType}};
{{/each}}
}

let counter = 0;

export function create{{modelName}}(overrides: Partial<{{modelName}}TestData> = {}): {{modelName}}TestData {
  counter++;
  return {
{{#each fields}}
    {{@key}}: overrides.{{@key}} ?? {{{this.defaultValue}}},
{{/each}}
  };
}

export function create{{modelName}}List(count: number, overrides: Partial<{{modelName}}TestData> = {}): {{modelName}}TestData[] {
  return Array.from({ length: count }, () => create{{modelName}}(overrides));
}
