// Generated by SpecForge — do not edit manually
// Page object for {{entityName}}

import type { Page, Locator } from "@playwright/test";

export class {{entityName}}Page {
  readonly page: Page;
  readonly heading: Locator;
{{#each locators}}
  readonly {{this.name}}: Locator;
{{/each}}

  constructor(page: Page) {
    this.page = page;
    this.heading = page.locator("h1");
{{#each locators}}
    this.{{this.name}} = page.locator("{{this.selector}}");
{{/each}}
  }

  async navigate() {
    await this.page.goto("/{{lowercase entityName}}");
  }

  async waitForLoad() {
    await this.page.waitForLoadState("networkidle");
  }
{{#each actions}}

  async {{this.name}}({{this.params}}) {
    {{this.body}}
  }
{{/each}}
}
