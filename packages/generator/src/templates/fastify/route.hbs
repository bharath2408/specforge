// Generated by SpecForge — do not edit manually
// Routes for {{modelName}} ({{endpoint.path}})

import type { FastifyInstance } from "fastify";

export default async function {{camelCase modelName}}Routes(app: FastifyInstance) {
{{#if (includes actions "list")}}
  // List all {{pluralize (lowercase modelName)}}
  app.get("{{endpoint.path}}", async (request, reply) => {
    // TODO: Implement list {{pluralize (lowercase modelName)}}
    return { data: [] };
  });

{{/if}}
{{#if (includes actions "get")}}
  // Get a single {{lowercase modelName}} by ID
  app.get("{{endpoint.path}}/:id", async (request, reply) => {
    const { id } = request.params as { id: string };
    // TODO: Implement get {{lowercase modelName}} by id
    return { data: null };
  });

{{/if}}
{{#if (includes actions "create")}}
  // Create a new {{lowercase modelName}}
  app.post("{{endpoint.path}}", async (request, reply) => {
    const body = request.body;
    // TODO: Implement create {{lowercase modelName}}
    reply.status(201);
    return { data: body };
  });

{{/if}}
{{#if (includes actions "update")}}
  // Update a {{lowercase modelName}}
  app.put("{{endpoint.path}}/:id", async (request, reply) => {
    const { id } = request.params as { id: string };
    const body = request.body;
    // TODO: Implement update {{lowercase modelName}}
    return { data: { id, ...body as object } };
  });

{{/if}}
{{#if (includes actions "delete")}}
  // Delete a {{lowercase modelName}}
  app.delete("{{endpoint.path}}/:id", async (request, reply) => {
    const { id } = request.params as { id: string };
    // TODO: Implement delete {{lowercase modelName}}
    reply.status(204);
    return;
  });

{{/if}}
}
