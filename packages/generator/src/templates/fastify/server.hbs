// Generated by SpecForge — do not edit manually
// Fastify server entry point

import Fastify from "fastify";
{{#each routes}}
import {{this.importName}}Routes from "./routes/{{this.fileName}}.js";
{{/each}}

const app = Fastify({
  logger: true,
});

// Register routes
{{#each routes}}
app.register({{this.importName}}Routes, { prefix: "{{../basePath}}" });
{{/each}}

const start = async () => {
  try {
    await app.listen({ port: 3000, host: "0.0.0.0" });
    console.log("Server running at http://localhost:3000");
  } catch (err) {
    app.log.error(err);
    process.exit(1);
  }
};

start();
