# ER Diagram

> Generated by SpecForge from {{spec.name}} v{{spec.version}}

```mermaid
erDiagram
{{#each models}}
    {{@key}} {
{{#each this.fields}}
        {{this.type}} {{@key}}
{{/each}}
    }
{{/each}}
```
