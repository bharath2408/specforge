// Generated by SpecForge — do not edit manually
// Tests for {{modelName}}

import { describe, it, expect } from "vitest";

describe("{{modelName}}", () => {
{{#each tests}}
  it("{{this.name}}", async () => {
    // Action: {{this.action}}
    {{#if this.input}}
    const input = {{{json this.input}}};
    {{/if}}

    // TODO: Implement test — call the API endpoint
    // Expected status: {{this.expect.status}}
    const response = { status: {{this.expect.status}}, body: {} as Record<string, unknown> };

    expect(response.status).toBe({{this.expect.status}});
    {{#if this.expect.body}}
    {{#each this.expect.body}}
    expect(response.body["{{@key}}"]).toBe({{{json this}}});
    {{/each}}
    {{/if}}
  });

{{/each}}
});
